---
# TEMPLATE: Pulumi Stack Config — GCP Development
# WHEN TO USE: Development configuration for GCP app-layer Pulumi deployment.
# PREREQUISITES: Pulumi stack initialized and non-production secrets configured with pulumi config set --secret.
# MATURITY: Stable
config:
  gcp:project: my-project-id
  gcp:region: us-central1
  gcp-gke:machineType: e2-small
  gcp-gke:nodeCount: "1"
